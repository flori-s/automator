<h1>New flow</h1>
<p class="muted">Minimal example: key <code>high_value_customer</code>, event <code>customer.updated</code>, builtin action <code>log</code> with <code>{"message":"matched"}</code>.</p>
<%= render "form" %>
